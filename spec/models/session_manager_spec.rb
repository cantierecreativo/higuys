require 'spec_helper'

describe SessionManager do
  # context 'if the session has no user user' do
  #   before do
  #     command.execute
  #   end

  #   it 'generates a user and stores it in the session' do
  #     expect(User.find(session[:user_id])).to be_present
  #   end
  # end
end

